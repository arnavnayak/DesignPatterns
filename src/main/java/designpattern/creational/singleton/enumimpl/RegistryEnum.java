package designpattern.creational.singleton.enumimpl;

public enum RegistryEnum {// enum we cant create subclass so no inheritance also enum object cannot be created
    INSTANCE;

    public void getConfiguration(){

    }
}
